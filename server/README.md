# Server (API)

## RU
Локальный API для работы с каталогом вопросов и заявками. Использует MySQL из `docker-compose.yml`.

## EN
Local API for catalog/questions and applications. Uses MySQL from `docker-compose.yml`.

## 中文
本地 API：题库与申请存储。使用 `docker-compose.yml` 中的 MySQL。

