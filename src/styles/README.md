# Styles

## RU
Глобальные стили и тема `styled-components` (`ThemeProvider`).

## EN
Global styles and `styled-components` theme (`ThemeProvider`).

## 中文
全局样式与 `styled-components` 主题（`ThemeProvider`）。

