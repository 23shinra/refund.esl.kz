# Utils

## RU
Доменная логика и “репозитории” (чистые функции). UI обращается сюда, но не знает, где/как хранятся данные.

## EN
Domain logic and repositories (pure functions). UI calls this layer without knowing where/how data is stored.

## 中文
领域逻辑与仓储层（纯函数）。UI 调用该层，但不关心数据存储方式。

