# Pages

## RU
Страницы (роуты). Используют репозитории/сервисы из `src/utils/` и собирают UI из `src/components/`.

## EN
Route-level pages. They call `src/utils/` and compose UI from `src/components/`.

## 中文
页面（路由层）。调用 `src/utils/`，并组合 `src/components/` 的 UI。

